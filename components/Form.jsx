import React from 'react';
import { View, StyleSheet } from 'react-native';

function Form(props) {
  return (
    <View style={styles.container}>
        <Text>Render a form</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {}
});

export default Form;